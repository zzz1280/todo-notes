# 复制本文件为 .env 并填入真实值（.env 已被 .gitignore 排除，不会提交）

# 数据库连接：SQLite 文件，相对 prisma/ 目录
DATABASE_URL="file:./dev.db"

# 后端服务端口
PORT=3001

# JWT 签名密钥：用下面命令生成一个随机的
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=请替换为随机密钥
