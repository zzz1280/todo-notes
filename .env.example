# 给 docker-compose.yml 用的环境变量（已被 .gitignore 排除，不会提交）
# 生成随机密钥：node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=请替换为随机密钥

# Docker 基础镜像仓库。国内网络构建时取消注释切换到镜像站：
# DOCKER_REGISTRY=docker.m.daocloud.io/library
